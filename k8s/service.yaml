apiVersion: v1
kind: Service
metadata:
  name: ecommerce-order-management-service
spec:
  type: LoadBalancer
  selector:
    app: ecommerce-order-management
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
